# Вопрос Кириллу: выбор цены в 454 спорных парах дублей (K3)

Дата подготовки: 2026-09-10. Статус: НЕ ОТПРАВЛЕН — вернуться к вопросу позже (по указанию пользователя).
Контекст: безопасная часть Бага B устранена (коммит `bc49227`): K1=485 + K2=73 → 572 клона скрыто, витринные цены не тронуты, верификация пройдена. Осталось 454 группы K3 (оба клона instock, цены разные).

Готовый текст (скопировать в Telegram целиком):

---

**По дублям размеров — разобрался.**

**Причина**: не синк и не баг — при апрельском импорте каталога у части кроссовок один размер пришёл **дважды с разными закупочными SKU** (последовательные артикулы поставщика, например 684948105 и 684948106). Это две разные партии/поставки с разными ценами. POIZON-синк эти записи не обновляет — проверил по метаданным.

**Что я уже почистил безопасно (без твоего участия):**
- 485 групп — цены у пар одинаковые → лишний клон скрыт (485 шт)
- 73 группы — в наличии только один из двух → оставил рабочий, лишний скрыт (87 шт)
- Итого **572 скрыто, витринные цены не изменились, всё проверено**

**Осталось 454 пары, где оба размера в наличии, но цены разные** (например 38-й: 7 490 ₽ и 9 190 ₽). Сейчас у покупателя при выборе такого размера цена показывается некорректно — нужно твоё решение, какую из двух цен оставлять:

1. **Оставлять более дешёвую** (обычно более свежая партия, конкурентнее для покупателя)
2. **Оставлять более дорогую** (больше маржа)
3. **Для товаров с привязкой к POIZON** (их из этих пар ~50) — брать цену из API, для остальных дешёвую

Скажи цифру 1/2/3 — применю за один прогон (с отчётом и возможностью отката). Полный список 454 пар в CSV могу прислать файлом.

---

## Материалы для применения (когда Кирилл ответит)
- Полный CSV пар с классами: `exports/dup-variations-classes-20260910.csv` (колонка `class` = K3).
- Точка отката: дамп `C:\Users\user\AppData\Local\Temp\opencode\leybo-pre-bugB2-20260910-*.sql` (150 MB, до K1/K2-фикса).
- Скрипты в `C:\Users\user\AppData\Local\Temp\opencode\`: `dup-classes.php` (переклассификация), `dup-apply.php` (скрытие порциями + чистка `_children` + `WC_Product_Variable::sync` + верификация диапазонов) — шаблон для K3-прогона: выбрать победителя по правилу Кирилла, скрыть проигравшего, тот же протокол (дамп → dry-run → применение → верификация витрины).
- Примечание: для варианта 3 (цена из POIZON) потребуется запрос к `leyboapi.vastubase.store/v1/products` по `_leybo_article` и обновление `_price` победителя перед скрытием проигравшего; стенд изолирован от внешнего HTTP — цену считать на хосте (PowerShell/curl) и передать скрипту списком.
